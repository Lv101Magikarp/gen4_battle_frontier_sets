{
  "name": "gen4-battle-frontier-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "copy-data": "mkdir -p public && cp ../data/sets.json public/sets.json && cp ../data/trainers.json public/trainers.json",
    "build:static": "npm run copy-data && tsc -b && VITE_DATA_MODE=static vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
